<Window x:Class="Waccy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Waccy"
        mc:Ignorable="d"
        Title="Waccy 剪贴板管理器" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="CanMinimize"
        Deactivated="Window_Deactivated"
        KeyDown="Window_KeyDown"
        Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="剪贴板历史" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListView Grid.Row="1" x:Name="HistoryListView" 
                  SelectionMode="Single"
                  BorderThickness="1"
                  MouseDoubleClick="HistoryListView_MouseDoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Preview}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1" Text="{Binding Timestamp, StringFormat=HH:mm:ss}" Foreground="Gray" Margin="10,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="ClearButton" Content="清空历史" Padding="10,5" Margin="0,0,10,0" Click="ClearButton_Click"/>
            <Button x:Name="CloseButton" Content="关闭" Padding="10,5" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
